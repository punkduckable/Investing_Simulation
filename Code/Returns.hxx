#ifndef RETURNS_HXX
#define RETURNS_HXX

namespace Returns {

struct Quarterly {
  // Note: both are reported as a percentage.
  double Capital_Gains;
  double Current_Income;
}; // struct Quarterly {

} // namespace Returns {

#endif
